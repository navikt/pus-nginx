version: 2.1
orbs:
  pus: navikt/pus-orb@0.0.26

workflows:
  version: 2
  commit:
    jobs:
      - pus/bygg-og-push-docker-image:
          context: pus
          filters:
            branches:
              only: master
